import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="app-header">
      <h1>RightsView GIS</h1>
      {/* We can add a logo here later */}
    </header>
  );
};

export default Header;
